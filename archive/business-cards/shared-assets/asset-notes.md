# Shared Assets

Copied here for local design/export use:

- `logo.png`
- `logo.webp`
- QR codes for both cards

Use `logo.png` first for most print editors unless they handle WebP cleanly.
If a printer/editor complains about transparency or color handling, export a flattened PNG from the design tool instead of using WebP directly.
